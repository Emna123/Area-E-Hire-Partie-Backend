@model IEnumerable<ApplicationTEST.Models.Formation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.diplome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.universite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.date_debut)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.date_fin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.diplome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.universite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date_debut)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date_fin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_formation">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_formation">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_formation">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
